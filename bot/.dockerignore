# В образ едут только исходники + go.mod/go.sum. Всё остальное — мимо,
# чтобы контекст сборки был лёгким и секреты не попали в слои.
.env
.env.*
!.env.example
*.md
Dockerfile
.dockerignore
.git
.gitignore

# Локально собранные бинари (если вдруг собирали `go build` на хосте).
/monbot
monbot
*.exe
